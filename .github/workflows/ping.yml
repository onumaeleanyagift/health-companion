name: Keep Health Companion Active

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: curl -f https://health-companion-9jwc.onrender.com || exit 1